# J-PLATPAT PDF公報テキスト化ツール（完全ローカル版）

J-PLATPAT（特許情報プラットフォーム）などからダウンロードした特許公報のPDFを、AIが読み込みやすい最適な形式のテキストファイルに変換するツールです。
2段組レイアウトの正しい順序でのテキスト化と、セキュリティに配慮した完全ローカル動作に対応しています。

---

## 主な機能と特徴

### 1. 2段組公報の正しい順序でのテキスト化
人間の目と同じように「左段を上から下まで読み、次に右段へ」という順序でテキストを再構成します。段落番号（`【０００１】` `【０００２】` など）が崩れることなく抽出できます。

### 2. 画像化された書誌事項のOCR抽出
J-PLATPATのPDFで画像化されている書誌事項（登録番号、出願日、特許権者、IPC分類など）をOCRで読み取り、「項目名：値」の構造化データとして整理します。
* **選択式出力:** OCR処理は若干時間がかかるため、テキストファイルに書誌事項を含めるかどうかをボタン一つで選択可能です。

### 3. セキュリティ安心の「完全ローカル」仕様
外部ライブラリの参照（CDN等）が一切ない、独立したローカルHTMLツールです。
* **オフライン動作:** インターネット接続がなくてもそのまま動きます。
* **外部通信なし:** 読み込んだPDFの内容や抽出データが外部のサーバーに送信されることは仕組み上あり得ません。社内のセキュリティポリシーが厳しい環境でも安心して導入・説明が可能です。

---

## 使い方

1. 本リポジトリのページ右上にある「Code」ボタン →「Download ZIP」からZIPファイルをダウンロードし、解凍します。
2. 解凍されたフォルダ内にある **`PDFテキスト変換ツール_OCR版2_完全ローカル版.html`** をブラウザ（Chrome / Edge など）でダブルクリックして開きます。Webサーバーは不要です。
3. 画面の指示に従ってPDF公報を読み込ませるだけで使用できます。

動作確認には、同梱のサンプルPDF（`local_assets/sample/動作確認用_特許公報サンプル.pdf`）が利用できます。

> [!CAUTION]
> **ファイル構成について**
> ライブラリ群はすべて同梱されています。解凍後に生成される `local_assets` フォルダは削除したり、HTMLファイルと離したりしないでください。移動やコピーを行う際は、必ずフォルダごとセットで行ってください。

---

## ファイル構成

```
PDFテキスト変換ツール_OCR版2_完全ローカル版.html … 本体（これをブラウザで開く）
local_assets/
  ├─ css/, js/, data/   … ツールの動作に必要なスクリプト・データ
  ├─ vendor/            … 同梱ライブラリ本体（PDF.js / Tesseract.js）
  ├─ licenses/          … 同梱ライブラリのライセンス本文
  ├─ sample/            … 動作確認用の特許公報サンプルPDF
  └─ README.md          … 技術的な詳細ドキュメント
```

OCRの操作手順や旧形式公報の扱い、トラブルシューティングなどの詳細は [local_assets/README.md](local_assets/README.md) を参照してください。

---

## 動作環境

* **推奨ブラウザ:** Google Chrome / Microsoft Edge（最新版）
* インターネット接続・Webサーバー・インストール作業はいずれも不要です。`file://` でHTMLを直接開くだけで動作します。
* 初回のOCR実行時は、学習データをメモリに展開するため、端末によっては開始まで数十秒かかることがあります。

---

## 同梱ライブラリ

本ツールは以下のオープンソースライブラリを同梱しています（バージョン固定）。

| ライブラリ | バージョン |
|---|---|
| PDF.js | 3.11.174 |
| Tesseract.js | 5.1.1 |
| tesseract.js-core | 5.1.1 |
| Tesseract 日本語・英語学習データ | 4.0.0 |

各ライセンスの本文は `local_assets/licenses/` に収録しています。

---

## ライセンス

本ツールは [Apache License 2.0](LICENSE) のもとで公開しています。商用・非商用を問わず自由に利用・改変・再配布できますが、再配布の際は `LICENSE` ファイルを同梱し、改変した場合はその旨を明示してください。

---

## 本ツールの限界・留意事項

* **図面の抽出はできません:** テキスト化の対象は文字情報のみです。図面をAIに検討させたい場合は、原本のPDFや画像ファイルを併用してください。
* **OCRは100%完全ではありません:** ほぼ正確に読み取れますが、濁点・半濁点の誤認（「ブラシ」と「プラシ」など）や、英数字の誤認（「0」と「O」など）が稀に発生します。重要な書誌事項は出力後に一度目視で確認することをおすすめします。
* **スキャン画像ベースの古い公報は対象外です:** 明細書本体のOCR機能は搭載していません。文字情報（テキストレイヤー）を持つPDF公報でご利用ください。
